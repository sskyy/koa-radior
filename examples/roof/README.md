# koa-radior with roof 0.5.x

## Develop

Start static page server:

```
npm install
npm install
npm run dev
```

Start API server:

```
node server.js
```

visit [http://localhost:8989](http://localhost:8989)

